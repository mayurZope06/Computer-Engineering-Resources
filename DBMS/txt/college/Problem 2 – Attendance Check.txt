🧮 Problem 2 – Attendance Check (Stud Table)
🎯 Objective:

If attendance < 75% → show “Term not granted” and set status = ‘D’
Else → show “Term granted” and set status = ‘ND’
Use control structure + exception handling.

✅ 1. Create Table
CREATE TABLE Stud (
  Roll INT PRIMARY KEY,
  Att NUMBER(5,2),
  Status CHAR(2)
);

✅ 2. Insert Sample Records
INSERT INTO Stud VALUES (1, 80, NULL);
INSERT INTO Stud VALUES (2, 60, NULL);
INSERT INTO Stud VALUES (3, 90, NULL);
COMMIT;

✅ 3. PL/SQL Block
DECLARE
    v_roll Stud.Roll%TYPE;
    v_att  Stud.Att%TYPE;
BEGIN
    -- Accept roll number from user
    v_roll := &roll_no;

    -- Retrieve attendance
    SELECT Att INTO v_att FROM Stud WHERE Roll = v_roll;

    -- Check condition
    IF v_att < 75 THEN
        DBMS_OUTPUT.PUT_LINE('Term not granted');
        UPDATE Stud SET Status = 'D' WHERE Roll = v_roll;
    ELSE
        DBMS_OUTPUT.PUT_LINE('Term granted');
        UPDATE Stud SET Status = 'ND' WHERE Roll = v_roll;
    END IF;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No record found for entered Roll No.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('Some unexpected error occurred.');
END;
/

✅ 4. Output Examples
Case 1:
Enter value for roll_no: 2
Term not granted

Case 2:
Enter value for roll_no: 1
Term granted

✅ 5. Check Updated Table
SELECT * FROM Stud;


Output:

ROLL	ATT	STATUS
1	80	ND
2	60	D
3	90	ND
💡 Viva Tips

IF-ELSE used → Control Structure.

EXCEPTION block handles runtime errors.

NO_DATA_FOUND occurs if no such roll number exists.

DBMS_OUTPUT.PUT_LINE is used to print messages on the SQL console.