💻 Problem 16 – MapReduce on Employee
use employee;

db.computer.insertMany([
  { name: "Amit", Salary: 30000 },
  { name: "Pooja", Salary: 25000 },
  { name: "Raj", Salary: 40000 }
]);

var mapFunc = function() { emit("total", this.Salary); };
var reduceFunc = function(key, values) { return Array.sum(values); };

db.computer.mapReduce(mapFunc, reduceFunc, { out: "totalSalary" });