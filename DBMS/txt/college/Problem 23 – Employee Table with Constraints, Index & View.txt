👨‍💼 Problem 23 – Employee Table with Constraints, Index & View
✅ Create Table with Constraints
CREATE TABLE Emp (
  Eno INT PRIMARY KEY,
  Ename VARCHAR(50) NOT NULL,
  Address VARCHAR(100) DEFAULT 'Nashik',
  Joindate DATE
);

✅ Add Column Post
ALTER TABLE Emp ADD Post VARCHAR(50);

✅ Insert Data
INSERT INTO Emp VALUES (101, 'Amit', 'Pune', '2022-05-12', 'Manager');
INSERT INTO Emp VALUES (102, 'Neha', DEFAULT, '2023-04-20', 'Clerk');
INSERT INTO Emp VALUES (103, 'Raj', 'Nashik', '2021-12-10', 'Tester');

✅ Create Index on Ename
CREATE INDEX idx_ename ON Emp(Ename);

✅ Create View (Ename & Salary)

(Assuming we add a salary column for demonstration)

ALTER TABLE Emp ADD Salary DECIMAL(10,2);

UPDATE Emp SET Salary=45000 WHERE Eno=101;
UPDATE Emp SET Salary=30000 WHERE Eno=102;
UPDATE Emp SET Salary=40000 WHERE Eno=103;

CREATE VIEW Emp_View AS
SELECT Ename, Salary FROM Emp;