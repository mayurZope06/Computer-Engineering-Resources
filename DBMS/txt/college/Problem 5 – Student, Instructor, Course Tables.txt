🧑‍🎓 Problem 5 – Student, Instructor, Course Tables
🎯 Objective: Use joins and aggregates.
✅ 1. Create Tables
CREATE TABLE Student (
  S_ID INT PRIMARY KEY,
  Name VARCHAR(50),
  Dept_Name VARCHAR(50),
  Tot_Cred INT
);

CREATE TABLE Instructor (
  T_ID INT PRIMARY KEY,
  Name VARCHAR(50),
  Dept_Name VARCHAR(50),
  Salary DECIMAL(10,2)
);

CREATE TABLE Course (
  Course_ID INT PRIMARY KEY,
  Title VARCHAR(50),
  Dept_Name VARCHAR(50),
  Credits INT
);

✅ 2. Insert Sample Data
INSERT INTO Student VALUES (1, 'Amol', 'Computer', 45);
INSERT INTO Student VALUES (2, 'Amit', 'IT', 50);
INSERT INTO Student VALUES (3, 'Sam', 'Computer', 55);

INSERT INTO Instructor VALUES (1, 'Prof. Mehta', 'Computer', 50000);
INSERT INTO Instructor VALUES (2, 'Prof. Rao', 'IT', 40000);
INSERT INTO Instructor VALUES (3, 'Prof. Shah', 'ENTC', 45000);

INSERT INTO Course VALUES (1, 'DBMS', 'Computer', 4);
INSERT INTO Course VALUES (2, 'DSA', 'IT', 3);
INSERT INTO Course VALUES (3, 'CN', 'Computer', 3);

✅ 3. Queries

i. Avg salary in depts > ₹42000

SELECT Dept_Name, AVG(Salary) AS Avg_Sal
FROM Instructor
GROUP BY Dept_Name
HAVING AVG(Salary) > 42000;


ii. Increase salary by 10% in Computer dept

UPDATE Instructor
SET Salary = Salary * 1.10
WHERE Dept_Name = 'Computer';


iii. Instructors not named ‘Amol’ or ‘Amit’

SELECT Name
FROM Instructor
WHERE Name NOT IN ('Amol', 'Amit');


iv. Students with ‘am’ substring

SELECT Name
FROM Student
WHERE LOWER(Name) LIKE '%am%';


v. Students from Computer dept taking DBMS

SELECT s.Name
FROM Student s
JOIN Course c ON s.Dept_Name = c.Dept_Name
WHERE c.Title = 'DBMS';